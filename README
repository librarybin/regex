core（是主逻辑）
fileio(i/o处理)
queue（是队列）
regex:（包含正则解释器所有逻辑）

（编译时，会有警告，这是由'\w'，'\d'等自定义转义字符造成，可以忽略）
主要思路：
	
		将正则符号分为两类
					
					1.匹配规则类（'*' '.' '?')
					2.新定义字符类(\d,\w,\t,[])

		根据朴素的字符匹配思路，匹配上则加入，不能匹配上则做出相应处理
		这里由于有新定义字符，因而我们这里得重载一下等号。


\a 对应 \w
\b 对应 \d
\v 对应 \s
这些都已经写好映射，使用时直接写\s \d \w


